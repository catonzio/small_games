import 'package:flutter/material.dart';

class PongMobile extends StatelessWidget {
  const PongMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}